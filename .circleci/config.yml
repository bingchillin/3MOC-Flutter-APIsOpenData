version: 2.1

jobs:
  build:
    environment:
      DART_SDK: stable
    docker: 
      - image: ghcr.io/cirruslabs/flutter:latest
    steps:
      - checkout
      - run: flutter doctor
      - run: flutter test
      - run: dart run discord-send-message

