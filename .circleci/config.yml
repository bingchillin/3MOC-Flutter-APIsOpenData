version: 2.1

jobs:
  build:
    environment:
      DART_SDK: stable
    docker: 
      - image: cirrusci/flutter
    steps:
      - checkout
      - run: flutter pub upgrade
      - run: dart run discord-send-message

